<h1>
  <%= "#{t("features.edit")} #{t("title.admin_title")} #{t("login.user")}" %>
</h1>

<%= render "form" %>

<%= link_to t("features.show"), admin_category_path %>
<%= link_to t("features.back"), admin_categories_path %>
