<% provide :title, t("users") %>
<h1>
  <%= t("users") %>
</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: Settings.gravatar.normal_size %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
